# Configuración del Proyecto

Esta carpeta reserva la configuración no sensible y parámetros declarativos del proyecto.
Actualmente la configuración operativa vive en `src/config.py` por compatibilidad con imports y tests.
